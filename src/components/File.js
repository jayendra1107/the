import React from "react";
import Avatar from "./Avatar";

const File = (props) => {
  return (
    <div className="file-container">
      <Avatar money={3000} />
    </div>
  );
};

export default File;
